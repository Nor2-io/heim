---
title: "Basic Python Application"
date: 2024-12-19
lastmod: 2025-06-12
description: This guide will help you get up and running with a basic Python application
---

import {
  FileTree,
  Card,
  CardGrid,
  LinkCard,
  Steps,
} from "@astrojs/starlight/components";

## Prerequisites

<LinkCard
  title="Getting Started with Heim"
  href="./../../../start-here/getting-started/"
/>

- Language-specific tools - [Install Heim Prerequisites](./../../../start-here/installation/#prerequisites)
- Heim cli and runtime - [Install Heim](./../../../start-here/installation/#installation)
- Text Editor - VsCode is a great option with language-specific plugins
- Terminal - Heim is accessed through its [command-line interface](./../../../cli/cli-reference/)

## Guide

<Steps>
    <ol>
        <li>
            :::caution[componentize-py]
            To build and deploy Python code you have to have componentize-py version >= 0.17.0. <br />A lower version will result in an error due to a breaking change in componentize-py
            :::
            <br/>
            
            Start by running `heim new` to create a project from a template. 
            
            <br/>
            
            :::note[template]
            In this guide we will be using the **python-http** template.
            :::

            :::tip[Code]
            The full code is available in our Github repository [**here**](https://github.com/Nor2-io/heim/tree/main/examples/Python)
            :::

            <br/>

            Use the up/down arrow keys to select the template and press enter:

            ``` sh frame=terminal title="Heim Cli"
            heim@system:~/repos$ heim new
            ? Select template:  ›
            csharp-http
            csharp-http-async
            ...
            ❯ python-http
            ...
            rust-http
            typescript-http
            ```

            <br/>

            :::tip
            The first part can be done by running one command:

            `heim new python-http`

            Use `heim template list` to get a list of available templates.
            :::

            <br/>

            <br/>

            Next, we will choose the [HTTP method](https://developer.mozilla.org/en-US/docs/Web/HTTP/Methods) that the application will respond on.

            <br/>

            For this we will choose the default value of `GET`:

            ``` sh frame=terminal title="Heim Cli"
            ✔ Select template:  · python-http
            ? Http trigger method POST/GET, Default (GET) ›
            ```

            <br/>

            :::note[HTTP method]

            You could set the application to use any [HTTP method](https://developer.mozilla.org/en-US/docs/Web/HTTP/Methods).

            :::

            <br/>

            <br/>

            Next, we need to choose the path that the application will listen on.

            Once the trigger path is set, we'll be able to go to the trigger path, and use the HTTP method we set to trigger the application.

            <br/>

            ``` sh frame=terminal title="Heim Cli"
            ✔ Select template:  · python-http
            ✔ Http trigger method POST/GET, Default · GET
            ? Http trigger path /??? , Default (/hello) › /my-python-application
            ```

            <br/>

            <br/>

            Last, we will need to set the name of the application.

            ``` sh frame=terminal title="Heim Cli"
            ✔ Select template:  · python-http
            ✔ Http trigger method POST/GET, Default · GET
            ✔ Http trigger path /??? , Default · /my-python-application
            ? Package name, (Required) › my-python-application

            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/component.toml""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/cli/run.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/sockets/ip-name-lookup.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/cli/terminal.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/sockets/network.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/clocks/timezone.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/sockets/world.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/nuget.config""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/filesystem/preopens.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/http/handler.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/http/proxy.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/io/poll.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/io/streams.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/cli/command.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/http/types.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/cli/stdio.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/my-python-application.csproj""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/world.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/.gitignore""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/random/insecure.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/filesystem/world.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/sockets/udp-create-socket.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/random/insecure-seed.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/application.toml""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/io/error.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/clocks/monotonic-clock.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/cli/exit.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/cli/imports.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/random/random.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/clocks/wall-clock.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/sockets/tcp-create-socket.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/sockets/tcp.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/random/world.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/cli/environment.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/sockets/udp.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/clocks/world.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/filesystem/types.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/io/world.wit""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/Program.cs""
            Rendered Template: ""/tmp/heimQ2z7DE/my-python-application/wit/deps/sockets/instance-network.wit""
            [00:00:00] ########################################      40/40      Success
            INFO [ Heim new ] New component created: Some("/home/heim/my-python-application")
            ```
            <br/>

            Once we've gone through all the previous steps, we'll have a project ready to go.

            <br/>

            <br/>

            <br/>

        </li>
        <li>

            <br/>

            Let's open the project in your text editor to check on what our new application looks like...

            <br/>

            Now, you will have an application folder with this structure:

            <FileTree>
                - \_\_pycache\_\_
                    - ...
                - wit/
                    - deps/
                    - wold.wit
                - app.py
                - .gitignore
                - **component.toml**
                - **application.toml**
            </FileTree>

            Take note of the marked configuration files. Let's take a look at these files, next.

            <br/>

            <br/>

            #### component.toml

            The [component.toml](./../../../start-here/configuration/componenttoml/) file, defines a component, and one or more components make up an application in Heim.

            <br/>

            `component.toml`
            ```toml
            name = "my-python-application"
            version = "0.1.0"

            [registry.metadata]
            authors = [""]
            description = "Description of my-python-application"
            license = ""
            categories = [""]

            [build.dev]
            build = "componentize-py -d ./wit -w wasi:http/proxy@0.2.1 componentize app -o my-python-application.wasm"
            wasm_path = "./my-python-application.wasm"

            [build.prod]
            build = "componentize-py -d ./wit -w wasi:http/proxy@0.2.1 componentize app -o my-python-application.wasm"
            wasm_path = "./my-python-application.wasm"
            ```
            _a simple component.toml, with explanatory comments_

            <br/>

            <LinkCard title="Read more about component.toml" href="./../../../start-here/configuration/componenttoml/" />

            <br/>

            <br/>

            #### application.toml

            The [application.toml](./../../../start-here/configuration/applicationtoml/) is what defines an application. An application.toml is required for deploying an application to Heim.

            <br/>

            `application.toml`
            ```toml
            name = "my-python-application"
            component_target = 'my-python-application:0.1.0'

            [[trigger]]
            type = 'http'
            path = '/my-python-application'
            method = 'GET'
            ```
            _a simple application.toml, with explanatory comments_

            <br/>

            <LinkCard title="Read more about application.toml" href="./../../../start-here/configuration/applicationtoml/" />

            <br/>

            <br/>

            <br/>

        </li>
        <li>

            <br/>

            Open `app.py` and take a look at the code we have:

            `app.py`
            ```python
            import asyncio
            import hashlib
            import poll_loop

            from nor2_world import exports
            from nor2_world.types import Ok
            from nor2_world.imports import types
            from nor2_world.imports.types import (
                Method_Get,
                Method_Post,
                Scheme,
                Scheme_Http,
                Scheme_Https,
                Scheme_Other,
                IncomingRequest,
                ResponseOutparam,
                OutgoingResponse,
                Fields,
                OutgoingBody,
                OutgoingRequest,
            )
            from poll_loop import Stream, Sink, PollLoop
            from typing import Tuple
            from urllib import parse


            class IncomingHandler(exports.IncomingHandler):
                def handle(self, request: IncomingRequest, response_out: ResponseOutparam) -> None:
                    loop = PollLoop()
                    asyncio.set_event_loop(loop)
                    loop.run_until_complete(handle_async(request, response_out))


            async def handle_async(
                request: IncomingRequest, response_out: ResponseOutparam
            ) -> None:
                response = OutgoingResponse(Fields.from_list([("content-type", b"application/json")]))

                response_body = response.body()

                ResponseOutparam.set(response_out, Ok(response))

                req = OutgoingRequest(Fields.from_list([]))
                req.set_scheme(Scheme_Https())
                req.set_authority('dummyjson.com')
                req.set_path_with_query('/recipes/1')

                resp = await poll_loop.send(req)

                stream = Stream(resp.consume())
                sink = Sink(response_body)
                while True:
                    chunk = await stream.next()
                    if chunk is None:
                        break
                    else:
                        await sink.send(chunk)

                sink.close()
            ```

            <br/>

            The app.py file contains boilerplate source code.

            <br/>

            <br/>

            Now, we want to start the application. Read on to learn the steps needed to start an application.

            <br/>

            <Steps>
                <ol>
                    <li>

                        <br/>

                        Run [heim start](./../../../cli/cli-reference/#start-options), to start the heim runtime:

                        ```sh title="heim runtime"
                        heim@system:~/repos$ heim start

                        Starting Runtime with Config:
                        ...

                        Http server listening on: http://127.0.0.1:3000
                        Heim portal: http://127.0.0.1:3000/heim/portal/
                        ```

                        <br/>

                        Note that the [Heim portal](./../../../start-here/heim-portal/) comes included with any Heim installation, and that it will be available on [`http://127.0.0.1:3000/heim/portal/`](http://127.0.0.1:3000/heim/portal/) once you've installed Heim.

                        <br/>

                        <br/>

                        <br/>

                    </li>
                    <li>

                        <br/>

                        To build the application and deploy it to our local-instance, we will run [heim deploy](./../../../cli/cli-reference/#deploy-options-module). By default, this will create a *debug* build, but we can change it to a *release* build with the `--release` flag:

                        ```sh title="heim cli"
                        heim@system:~/repos$ heim deploy

                        INFO [ heim_cli::wasm::build ] Building Component: my-python-application
                            Restore complete (0,6s)
                            my-python-application succeeded (4,5s)

                            Build succeeded in 7,7s
                        INFO [ heim_registry::registry ] "my-python-application":"0.1.0":cb4abe161542a7b146fe27c5f5cdb60970d68a369f5703bde252664ddbe2eedf
                        INFO [ Heim::Deploy ] Heim application `my-python-application` is now built and written to registry.
                        INFO [ Heim::Pipeline ] Starting pipeline 'Deploy application'
                        INFO [ Heim::Pipeline ] Storing unoptimized component state
                        INFO [ Heim::Pipeline ] Checking if defined regions in app definition is valid
                        INFO [ Heim::Pipeline ] Continuing with localhost region heim-localhost
                        INFO [ Heim::Pipeline ] Getting region specific application environment values
                        INFO [ Heim::Pipeline ] Set environment values for region 'heim-localhost'
                        INFO [ Heim::Pipeline ] Getting region specific application scaling values
                        INFO [ Heim::Pipeline ] Set scaling values for region 'heim-localhost'
                        INFO [ Heim::Pipeline ] Getting capabilities
                        INFO [ Heim::Pipeline ] Set capabilities values for region 'heim-localhost'
                        INFO [ Heim::Pipeline ] Getting triggers
                        INFO [ Heim::Pipeline ] Set trigger values for region 'heim-localhost'
                        INFO [ Heim::Pipeline ] Component environment values are valid for all regions
                        INFO [ Heim::Pipeline ] Checking for region with changes to build
                        INFO [ Heim::Pipeline ] Region "heim-localhost" has been changed and will be built
                        INFO [ Heim::Pipeline ] Storing component and optimized component
                        INFO [ Heim::Pipeline ] Stored component at ''
                        INFO [ Heim::Pipeline ] Stored optimized component at ''
                        INFO [ Heim::Pipeline ] Storing region states
                        INFO [ Heim::Pipeline ] Stored state for region(s) heim-localhost successfully
                        INFO [ Heim::Pipeline ] Creating and caching application(s)
                        INFO [ Heim::Pipeline ] Creating and caching application(s) for region(s) heim-localhost
                        INFO [ Heim::Pipeline ] Created application with hash 8aba8efca7c309fe7cafce25df43fe78bbe9d0479f9b709cea97f9d507048a11 for region(s) heim-localhost
                        INFO [ Heim::Pipeline ] Application has been deployed for region(s) heim-localhost
                        INFO [ Heim::Pipeline ] Pipeline 'Deploy application' finished with status 'Success'
                        INFO [ Heim::Deploy ] Heim deploy pipeline was successful
                        INFO [ Heim::Deploy ] Application is accaccessible at:
                        INFO [ Heim::Deploy ] [GET] http://127.0.0.1:3000/my-python-application
                        INFO [ Heim::Deploy ] Written and deployed application `my-python-application`.
                        ```

                        <br/>

                        <br/>

                        From here, you can see that we are able to access the application on `http://127.0.0.1:3000/my-python-application`:

                        ```sh title="curl"
                        heim@system:~/repos$ curl http://127.0.0.1:3000/my-python-application
                        Hello World!
                        ```

                        <br/>

                        <br/>

                        To deploy the application to the cloud instance, we will run [heim deploy](./../../../cli/cli-reference/#deploy-options-module) with the `--cloud` flag.
                        This will generate a *release* build as default, but it can be changed to a *debug* with the the `--dev` flag.

                        This will deploy the application to your subdomain at `my-subdomain.cloud.heim.dev`

                        ```sh title="heim cli"
                        heim@system:~/repos$ heim deploy --cloud

                        INFO [ heim_registry::registry ] "my-python-application":"0.1.0":02f6d7d3c34210438f293efc1ff2f8e9494f783a402e5dcbe6a308fea40d870e
                        INFO [ Heim::Deploy ] Heim application `my-python-application` is now built and written to registry.
                        INFO [ Heim::Pipeline ] Starting pipeline 'Deploy application'
                        INFO [ Heim::Pipeline ] Storing unoptimized component state
                        INFO [ Heim::Pipeline ] Checking if defined regions in app definition is valid
                        INFO [ Heim::Pipeline ] Continuing with localhost region heim-localhost
                        INFO [ Heim::Pipeline ] Getting region specific application environment values
                        INFO [ Heim::Pipeline ] Set environment values for region 'heim-localhost'
                        INFO [ Heim::Pipeline ] Getting region specific application scaling values
                        INFO [ Heim::Pipeline ] Set scaling values for region 'heim-localhost'
                        INFO [ Heim::Pipeline ] Getting capabilities
                        INFO [ Heim::Pipeline ] Set capabilities values for region 'heim-localhost'
                        INFO [ Heim::Pipeline ] Getting triggers
                        INFO [ Heim::Pipeline ] Set trigger values for region 'heim-localhost'
                        INFO [ Heim::Pipeline ] Component environment values are valid for all regions
                        INFO [ Heim::Pipeline ] Checking for region with changes to build
                        INFO [ Heim::Pipeline ] Region "heim-localhost" has been changed and will be built
                        INFO [ Heim::Pipeline ] Optimizing base component
                        INFO [ Heim::Pipeline ] Optimized base component, size before optimization: 2881425 bytes, size after optimization: 2278867 bytes
                        INFO [ Heim::Pipeline ] Storing component and optimized component
                        INFO [ Heim::Pipeline ] Stored component at ''
                        INFO [ Heim::Pipeline ] Stored optimized component at ''
                        INFO [ Heim::Pipeline ] Storing region states
                        INFO [ Heim::Pipeline ] Stored state for region(s) heim-localhost successfully
                        INFO [ Heim::Pipeline ] Creating and caching application(s)
                        INFO [ Heim::Pipeline ] Creating and caching application(s) for region(s) heim-localhost
                        INFO [ Heim::Pipeline ] Created application with hash 62fd854418968a7c0cc383e962382dfc8e2d420b7fa5a4d48c6058ebb8544a05 for region(s) heim-localhost
                        INFO [ Heim::Pipeline ] Application has been deployed for region(s) heim-localhost
                        INFO [ Heim::Pipeline ] Pipeline 'Deploy application' finished with status 'Success'
                        INFO [ Heim::Deploy ] Heim deploy pipeline was successful
                        INFO [ Heim::Deploy ] Application is accaccessible at:
                        INFO [ Heim::Deploy ] [GET] https://my-subdomain.cloud.heim.dev/my-python-application
                        INFO [ Heim::Deploy ] Written and deployed application `my-python-application`.
                        ```

                        <br/>

                        <br/>

                        And, in the heim runtime you will see something like this, if you started the runtime with the `--verbose` flag::

                        ```log "app_id" "uri" "path" "method" "domain"
                        INFO WasmComponentHandler{
                            app_id="localhost:00000000-0000-0000-0000-000000000000:00000000-0000-0000-0000-000000000000:my-python-application:bbcb756cbf72e572fd47f3c299f44e1d083522aaa2696c3b0b24653deb884d3c"
                            uri=/my-python-application
                        }:
                        heim_runtime::services::handlers::wasm_component_handler: 48:
                        app_id="localhost:00000000-0000-0000-0000-000000000000:00000000-0000-0000-0000-000000000000:my-python-application:bbcb756cbf72e572fd47f3c299f44e1d083522aaa2696c3b0b24653deb884d3c"
                        uri=/my-python-application method=GET
                        trigger=Trigger {
                            app_id: AppId("localhost:00000000-0000-0000-0000-000000000000:00000000-0000-0000-0000-000000000000:my-python-application:bbcb756cbf72e572fd47f3c299f44e1d083522aaa2696c3b0b24653deb884d3c"),
                            path: "/my-python-application",
                            method: "GET",
                            function_target: None,
                            hash: "",
                            domain: "127.0.0.1"
                        }
                        ```

                        <br/>

                        The Heim runtime output above summarizes all the basic information about your application.

                        <br/>

                        <br/>

                    </li>
                </ol>
            </Steps>
        </li>
        <li>

            <br/>
            Let's add an environment variable to our `component.toml`.:

            `component.toml`
            ```diff lang=toml
            name = "my-python-application"
            version = "0.1.0"

            [registry.metadata]
            authors = [""]
            description = "Description of my-python-application"
            license = ""
            categories = [""]

            [build.dev]
            build = "npm run build"
            wasm_path = "./dist/my-python-application.wasm"

            [build.prod]
            build = "npm run build"
            wasm_path = "./dist/my-python-application.wasm"

            +[input.env]
            +HELLO_MESSAGE = { type = "String" }
            ```

            <br/>

            <br/>

            While we're at it we can change the `HTTP method` to `POST`, and set the `path` to a new value. Furthermore, we'll set a value to the `HELLO_MESSAGE` environment variable:

            `application.toml`
            ```diff lang=toml
            name = "my-python-application"
            component_target = 'my-python-application:0.1.0'

            [[trigger]]
            type = 'http'
            +path = '/mypath'
            -path = '/my-python-application'
            +method = 'POST'
            -method = 'GET'

            +[env]
            +HELLO_MESSAGE = "you"
            ```

            <br/>

            <br/>

            Finally, we'll update the code to use the new environment variable, and modify our code to expect a JSON body, and return another JSON body:

            `app.py`
            ```diff lang=python
            import asyncio
            import hashlib
            import poll_loop
            import json
            import os

            from nor2_world import exports
            from nor2_world.types import Ok
            from nor2_world.imports import types
            from nor2_world.imports.types import (
                Method_Get,
                Method_Post,
                Scheme,
                Scheme_Http,
                Scheme_Https,
                Scheme_Other,
                IncomingRequest,
                ResponseOutparam,
                OutgoingResponse,
                Fields,
                OutgoingBody,
                OutgoingRequest,
            )
            from poll_loop import Stream, Sink, PollLoop
            from typing import Tuple
            from urllib import parse


            class IncomingHandler(exports.IncomingHandler):
                def handle(self, request: IncomingRequest, response_out: ResponseOutparam) -> None:
                    loop = PollLoop()
                    asyncio.set_event_loop(loop)
                    loop.run_until_complete(handle_async(request, response_out))


            async def handle_async(
                request: IncomingRequest, response_out: ResponseOutparam
            ) -> None:
                request_stream = Stream(request.consume())
                chunks = []
                while True:
                    chunk = await request_stream.next()
                    if chunk is None:
                        break
                    chunks.append(chunk)
                body_bytes = b"".join(chunks)

                try:
                    data = json.loads(body_bytes.decode("utf-8"))
                    message_value = data.get("message", "")
                except Exception:
                    message_value = ""

                env_var = os.getenv("HELLO_MESSAGE")

                response_data = json.dumps(f"{message_value}, {env_var}!").encode("utf-8")

                headers = Fields.from_list([("content-type", b"application/json")])

                response = OutgoingResponse(headers)

                response_body = response.body()

                ResponseOutparam.set(response_out, Ok(response))

                sink = Sink(response_body)
                await sink.send(response_data)
                sink.close()
            ```

            <br/>

            <br/>

            Since the runtime is already running, we just need to build and deploy the application again, for our changes to take effect.

            ```sh title="heim  cli"
            heim@system:~/repos$ heim deploy
            INFO [ heim_cli::wasm::build ] Building Component: my-python-application
            Restore complete (0,4s)
            my-python-application succeeded (1,6s) 

            Build succeeded in 3,3s
            INFO [ heim_cli::wasm::wasmodule ] my-python-application:89ce0f774f6c25038dad4113ab83b324b800799d57af630998a2f35744595229
            INFO [ Heim deploy ] Built and published my-python-application/89ce0f774f6c25038dad4113ab83b324b800799d57af630998a2f35744595229
            INFO [ Heim deploy ] Deployed to Heim-portal-backend my-python-application/89ce0f774f6c25038dad4113ab83b324b800799d57af630998a2f35744595229
            INFO [ Heim deploy ] Access at: http://127.0.0.1:3000/mypath with Method POST
            INFO [ Heim::Publish ] Written and deployed module my-python-application to runtime for profile local
            ```

            <br/>

            <br/>

            ```sh title="curl"
            heim@system:~/repos$ curl.exe -X POST "http://127.0.0.1:3000/mypath" -H "Content-Type: application/json" -d '{\"message\":\"Hello\"}'
            Hello, you!
            ```

            <br/>

            <br/>

            <br/>
        </li>
        <li>

            <br/>

            When we are done, we can use the shortcut `Ctrl+C` to shut down the runtime.

            <br/>

            To clean up any resources from the guide you can run `heim clear`. This will remove all data from the cache.

            <br/>

            :::caution[heim clear]
            You will need to redeploy the application the next time you want to run it, if you use the `heim clear` command.
            :::

            :::tip[Code]
            The full code is available in our Github repository [**here**](https://github.com/Nor2-io/heim/tree/main/examples/Python)
            :::

            <br/>

            <br/>

            <br/>

        </li>
    </ol>

</Steps>

<br />

<br />

<br />

---

<br />

## Summary

<br />

1. - run `heim new`
   - select the python-http `template`
   - choose `HTTP method`
   - choose trigger `path`
   - set application `name`

   {" "}

   <br />
   <br />

2. - open the created application folder/project in your text editor

   _Take note of the configuration files:_

   :::note[component.toml]
   component.toml defines a component.

   An application is made up by one or more components.
   :::

   :::note[application.toml]
   application.toml defines an application.
   :::

   <br />

   <br />

3. - open `app.py`:

   :::note[app.py]
   app.py contains boilerplate source code for your application.
   :::

   {" "}

   <br />

   - run `heim start` to start the Heim runtime

   :::note[Heim portal]
   The Heim portal comes included with a Heim installation.

   Once the Heim runtime is started, the Heim portal is available on [http://127.0.0.1:3000/heim/portal/](http://127.0.0.1:3000/heim/portal/).
   :::
   :::

   {" "}

   <br />

   - run `heim deploy` to build the application.

   :::note[heim deploy --cloud --dev]
   The `heim deploy --cloud` creates a _development_ build.

   Just type `heim deploy --cloud` to create a _release_ build.
   :::

   {" "}

   <br />

   - run `heim deploy --local ${application_name}` to deploy the application while targeting the local runtime

   {" "}

   <br />

   :::note[access application]
   The application is accessible on:

   `http://127.0.0.1:3000/${application_path}`
   :::

   :::tip[heim runtime]
   The Heim runtime will print out relevant information about the application.
   :::

   <br />

   <br />

4. - edit the `package.json` file to add useful dependencies
   - edit the `component.toml` file to add _environment variables_
   - edit the `application.toml` file to change the _HTTP method_
   - edit the `application.toml` file to change the _trigger path_
   - edit the `application.toml` file to set environment variable values
   - edit the `app.py` file to use new environment variables
   - edit the `app.py` file to set up return and accept values
   - _deploy_ the application again to see your changes

   {" "}

   <br />

   {" "}

   <br />

5. - use the `Ctrl+C` shortcut to shut down the Heim runtime
   - use the `heim clear` command to remove all data from cache

   {" "}

   <br />

   :::caution[heim clear]
   You will need to redeploy the application the next time you want to run it, if you use the `heim clear` command.
   :::

<br />

<br />

---

<br />

## References

- [heim cli](./../../../cli/cli-reference/)
- [component.toml](./../../../start-here/configuration/componenttoml/)
- [application.toml](./../../../start-here/configuration/applicationtoml/)
